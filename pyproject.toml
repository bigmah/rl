[project]
name = "rl"
version = "0.1.0"
description = "PufferLib 5.0 experiments: C envs trained with an MLX port of its trainer"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mlx>=0.32.2",
    "numpy",
]

[tool.uv]
package = false
