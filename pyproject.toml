[project]
name = "rl"
version = "0.1.0"
description = "PufferLib experiments: a 2D platformer env trained with PuffeRL"
requires-python = ">=3.12,<3.13"
dependencies = ["pufferlib"]

[tool.uv]
package = false

[tool.uv.sources]
pufferlib = { path = "vendor/PufferLib", editable = true }
